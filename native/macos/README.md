# Korean Tech Wire macOS field test

The app runs the canonical KTW core and loopback newsroom with `KOREAN_TECH_WIRE_DATA_DIR`. Its default mutable state is `~/Library/Application Support/Korean Tech Wire/`; it never uses a production database or secrets.
