# Windows symmetry

Any later Windows package must use the canonical core and loopback newsroom, honour `KOREAN_TECH_WIRE_DATA_DIR`, and default mutable state to `%LOCALAPPDATA%\Korean Tech Wire\`. This change does not introduce a Windows executable.
